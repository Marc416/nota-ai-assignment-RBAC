jwt:
  secret-key: ${JWT_SECRET_KEY:secret}  # ??? ??

spring:
  datasource:
    url: jdbc:mysql://localhost:13306/nota
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: joonhee
    password: 1234
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.notaaiassignmentrdbac: DEBUG
    org.springframework.jdbc.core: DEBUG
    org.hibernate.SQL: DEBUG

